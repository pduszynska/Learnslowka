LexiForge v4

Built-in words: 93

Upload index.html to GitHub Pages.

New/fixed:
- Includes all words from the advanced list plus the earlier export words.
- Import page now has a clearly visible Upload JSON file input.
- Selecting a JSON file imports automatically.
- Backup page has a clearly visible Upload backup JSON file input.
- Export backup downloads words + progress.

Use GitHub Pages or another hosted URL. Do not open the HTML through iPhone Files preview.
