LexiForge v7 Safari Safe

Built-in words: 93

This version is made to work more reliably on iPad/iPhone Safari:
- avoids newer JS syntax that can break older Safari builds
- shows an error message inside the app if something fails
- mode buttons render more safely
- voice works only after a real tap, which Safari requires
- upload JSON, backup, games, analytics, XP and pronunciation included

Upload index.html to GitHub Pages.
Open via the hosted URL, not iPhone/iPad Files preview.

If replacing an older version on GitHub Pages:
1. Upload this index.html over the old one.
2. Wait 1-3 minutes.
3. On iPad Safari, reload the page.
4. If needed, open the app and tap Reset to built-in words.

New in v7:
- Prevents adding a word manually if it already exists.
- Upload/import skips duplicate words and reports how many were skipped.
